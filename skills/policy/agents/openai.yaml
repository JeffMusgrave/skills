interface:
  display_name: "Policy"
  short_description: "Plan and run Code Policy checks safely"
  default_prompt: "Use $policy to show the available verification levels and help me choose the right one."
policy:
  allow_implicit_invocation: false
